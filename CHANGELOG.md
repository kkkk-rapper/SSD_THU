# 更新日志

本项目的主要变更记录。日期格式 YYYY-MM-DD。

## [0.2.0] - 2026-05-22

围绕「表面划痕影响」的 A/B 对照研究，建立完整的「电场 → 信号 → 能谱」链路。

### 新增

- **双配置 A/B 对照**：`config.yaml`（有划痕）与 `config_noscratch.yaml`（无划痕基线），除划痕外逐字段一致。
- `field.jl` —— 1000 V 电场 / 电势截面图（CUDA 加速，坐标轴 mm）。
- `signal.jl` —— 读取 Geant4 `hits.csv`，多线程逐事件漂移电荷，生成 p+ 读出电极波形与能谱；全部波形批量保存，供后期按能量段查看。
- `render_geometry.jl` —— 几何 3D 总览 + 2D 点分类截面图（如实反映 CSG 布尔结果）。
- `verify_configs.jl` —— 校验两份配置仅「划痕」一项不同。
- `compare_spectra.jl` —— 有 / 无划痕能谱对比（线性坐标）。
- `view_waveforms.jl` —— 按测得能量段调看已保存的波形。
- 项目文档：`README.md`、`CHANGELOG.md`、`CLAUDE.md`。

### 变更

- 划痕几何改用 **`Polycone` 回转体**建模：旋转到全局 x 轴后与晶体本体求交，只保留落在晶内（z ≤ 10 mm）的下半部 —— 取代早期的多球叠加，单图元、锥面光滑、网格更省。
- `simulate.jl` 重构：支持命令行参数（配置文件 / 运行阶段），结果按配置分目录输出。
- 电荷漂移与场求解全面接入**多线程**与 **CUDA**；修正 3D 模型 `max_tick_distance` 的用法（必须用元组，否则 φ 网格爆炸）。

### 移除

- 早期的 2D 轴对称项目 `ppc/` —— 2D 无法放置偏心划痕，已弃用。

### 说明

- 确认结论：在无电荷陷阱、完全耗尽的理想模型下，划痕只改变脉冲波形的形状 / 时序，**不改变能谱**。
- 波形二进制 `*.bin` 等大体积产物加入 `.gitignore`（超 GitHub 文件大小限制）。

## [0.1.0] - 2026-05-20

- PPC 高纯锗探测器基础仿真（SolidStateDetectors.jl）：电势 / 电场、耗尽电压、信号波形。
